<header class="intro">
        <div class="intro-body">
            <div class="container">
                <div class="row">
                    <div class="col-md-8 col-md-offset-2">
                        <h1 class="brand-heading">   Precio: $<%= @propiedad.precio %></h1>
                        <p class="intro-text">   Arrendador: <%= Usuario.find_by_id(@propiedad.usuario_id).nombre %></p>
                        <p class="intro-text">    Direccion: <%= @propiedad.direccion %></p>
                        <p class="intro-text">  Descripción: <%= @propiedad.descripcion %></p>   
                        <p class="intro-text">  Imagen: <%= @propiedad.imagen %></p>   
                        <p class="intro-text">     Apartada: <%= @propiedad.apartada %></p>
                        <%= link_to "Regresar a lista de propiedades", root_url %>                    
                    </div>
                </div>
            </div>
        </div>
</header>